# Bedford generated project data

This directory is the local output boundary for Bedford drawing intake. Generated manifests, catalog copies, and reports are ignored by Git. Placeholder files preserve the expected directory structure.

The current intake pass creates document identities and classification reports only. Rooms, finishes, schedules, objects, and relationships are not claimed complete.
