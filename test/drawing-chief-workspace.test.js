import test from'node:test';import assert from'node:assert/strict';import{readFileSync}from'node:fs';
const app=readFileSync(new URL('../src/app.js',import.meta.url),'utf8'),css=readFileSync(new URL('../src/app.css',import.meta.url),'utf8');
test('Ask Chief opens a dock without switching workspaces or rerendering PDF',()=>{const handler=app.slice(app.indexOf("if (button.hasAttribute('data-drawing-ask'))"),app.indexOf("if (button.hasAttribute('data-drawing-ask'))")+900);assert.match(handler,/chiefDrawingDock\.open/);assert.doesNotMatch(handler,/showMissionControlView|paintDrawingPage|renderPdfPage/);assert.match(app,/drawingRerenderCount:0/);});
test('dock reuses active conversation and drawing-context answer generation',()=>{assert.match(app,/engine\.activeConversation\(\)/);assert.match(app,/engine\.ask\(value,state\(\)\.settings\.mode,\{drawingContext:activeChiefDrawingContext/);assert.match(app,/chiefDrawingDockComposer/);assert.match(css,/\.mc-chief-drawing-dock/);});
test('return state preserves viewport, selection, coverage review, dock, and conversation',()=>{assert.match(app,/captureDrawingSupportReturnState/);assert.match(app,/selectedObjectIds:\[\.\.\.selectedDrawingObjectIds\]/);assert.match(app,/coverageReview:\{open:drawingCoverageReviewMode/);assert.match(app,/chiefDock:chiefDrawingDock\.state/);assert.match(app,/conversationId:engine\.activeConversation/);assert.match(app,/restoreDrawingSupportReturnState/);});
test('action audit decorates representative visible drawing controls',()=>{for(const id of['previous-page','next-page','fit-page','fit-width','rotate-clockwise','reset-view','ask-chief','open-coverage-review','clear-selection','previous-object','next-object','center-object','zoom-object'])assert.match(app,new RegExp(`'${id}'`));assert.match(app,/auditDrawingActions\(controlsFromDrawingRoot/);});
test('dock cards use action router and never create PDF proxies',()=>{assert.match(app,/buildChiefDrawingCards/);assert.match(app,/drawingActionRouter\.execute\(button\.dataset\.chiefCardAction/);const cards=readFileSync(new URL('../src/chief-drawing-cards.js',import.meta.url),'utf8');assert.doesNotMatch(cards,/openPdfBlob|PDFDocumentProxy|renderPdfPage/);});
